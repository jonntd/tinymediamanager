name: Docker Build

on:
  workflow_dispatch:
    inputs:
      platforms:
        description: 'Target platforms'
        required: true
        default: 'linux/amd64,linux/arm64'
        type: choice
        options:
        - linux/amd64
        - linux/arm64
        - linux/amd64,linux/arm64
  workflow_run:
    workflows: ["Multi-Platform CI/CD Pipeline"]
    types:
      - completed
    branches: [ devel ]
    # 只在Multi-Platform CI/CD Pipeline成功完成时触发

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  TMM_VERSION: "5.2.0"

jobs:
  # 检查构建环境
  check-environment:
    name: Check Environment
    runs-on: ubuntu-latest
    outputs:
      has-upstream-artifacts: ${{ steps.check.outputs.has-upstream }}
      build-type: ${{ steps.check.outputs.build-type }}
      should-build: ${{ steps.check.outputs.should-build }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Check for upstream artifacts
      id: check
      run: |
        echo "🔍 检查构建环境..."
        
        # 检查是否应该构建（防止重复触发）
        if [ "${{ github.event_name }}" = "workflow_run" ]; then
          if [ "${{ github.event.workflow_run.conclusion }}" = "success" ]; then
            echo "should-build=true" >> $GITHUB_OUTPUT
            echo "has-upstream=true" >> $GITHUB_OUTPUT
            echo "build-type=upstream" >> $GITHUB_OUTPUT
            echo "✅ 使用上游构建产物"
          else
            echo "should-build=false" >> $GITHUB_OUTPUT
            echo "has-upstream=false" >> $GITHUB_OUTPUT
            echo "build-type=none" >> $GITHUB_OUTPUT
            echo "❌ 上游工作流未完成，跳过构建"
          fi
        elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "should-build=true" >> $GITHUB_OUTPUT
          echo "has-upstream=false" >> $GITHUB_OUTPUT
          echo "build-type=local" >> $GITHUB_OUTPUT
          echo "✅ 手动触发构建"
        else
          echo "should-build=false" >> $GITHUB_OUTPUT
          echo "has-upstream=false" >> $GITHUB_OUTPUT
          echo "build-type=none" >> $GITHUB_OUTPUT
          echo "⚠️ 未知触发源，跳过构建"
        fi

  # 构建 AMD64 Docker 镜像
  docker-build-amd64:
    name: Build Docker Image (AMD64)
    runs-on: ubuntu-latest
    needs: check-environment
    # 只在明确需要构建时执行（防止重复触发）
    if: needs.check-environment.outputs.should-build == 'true'
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: devel

    - name: Download Linux AMD64 artifact (if available)
      if: needs.check-environment.outputs.has-upstream-artifacts == 'true' && github.event.workflow_run.conclusion == 'success'
      uses: actions/download-artifact@v4
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        run-id: ${{ github.event.workflow_run.id }}
        pattern: "tinyMediaManager-linux-amd64*"
        path: artifacts

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Prepare Docker context
      run: |
        echo "🏗️ Preparing Docker context for AMD64..."
        mkdir -p target/docker

        # 调试信息
        echo "📁 当前工作目录: $(pwd)"
        echo "📁 src/docker目录内容:"
        ls -la src/docker/
        echo "📁 当前目录所有文件:"
        find . -name "*Dockerfile*" -type f

        # 显示下载的构建产物
        echo "📁 下载的构建产物:"
        ls -la . | grep -E "(tinyMediaManager|tinymediamanager)" || echo "未找到构建产物目录"

        # 检查是否有构建产物
        HAS_ARTIFACTS=false

        # 首先检查是否有下载的构建产物目录（GitHub Actions download-artifact 会创建目录）
        ARTIFACT_DIRS=$(find . -maxdepth 1 -type d -name "*linux*amd64*" -o -name "*tinyMediaManager*amd64*" | head -5)

        if [ -n "$ARTIFACT_DIRS" ]; then
          echo "📦 找到构建产物目录:"
          echo "$ARTIFACT_DIRS"

          # 处理第一个找到的目录
          ARTIFACT_DIR=$(echo "$ARTIFACT_DIRS" | head -1)
          echo "📂 处理目录: $ARTIFACT_DIR"

          # 显示目录内容
          echo "📁 目录内容:"
          ls -la "$ARTIFACT_DIR"

          # 查找目录中的构建产物文件
          BUILD_FILES=$(find "$ARTIFACT_DIR" -type f \( \
            -name "*.tar.xz" -o \
            -name "*.tar.gz" -o \
            -name "*.zip" -o \
            -name "*tinyMediaManager*" \
          \) | head -10)

          if [ -n "$BUILD_FILES" ]; then
            echo "✅ 找到构建产物文件:"
            echo "$BUILD_FILES"

            # 处理第一个找到的文件
            BUILD_FILE=$(echo "$BUILD_FILES" | head -1)
            echo "📦 处理文件: $BUILD_FILE"

            # 创建目标目录
            mkdir -p target/docker

            # 根据文件类型处理
            case "$BUILD_FILE" in
              *.tar.xz)
                echo "📦 解压 tar.xz 文件: $BUILD_FILE"
                tar xJf "$BUILD_FILE" -C target/docker/ || {
                  echo "❌ tar.xz 解压失败"
                  exit 1
                }
                ;;
              *.tar.gz)
                echo "📦 解压 tar.gz 文件: $BUILD_FILE"
                tar xzf "$BUILD_FILE" -C target/docker/ || {
                  echo "❌ tar.gz 解压失败"
                  exit 1
                }
                ;;
              *.zip)
                echo "📦 解压 zip 文件: $BUILD_FILE"
                unzip -q "$BUILD_FILE" -d target/docker/ || {
                  echo "❌ zip 解压失败"
                  exit 1
                }
                ;;
              *)
                echo "📋 直接复制文件: $BUILD_FILE"
                mkdir -p target/docker/tinyMediaManager
                cp "$BUILD_FILE" target/docker/tinyMediaManager/
                ;;
            esac

            HAS_ARTIFACTS=true
          else
            echo "⚠️ 目录中未找到可识别的构建产物文件"
            echo "📁 尝试直接复制整个目录内容..."

            # 如果没有找到特定文件，尝试复制整个目录
            if [ -d "$ARTIFACT_DIR" ]; then
              mkdir -p target/docker
              cp -r "$ARTIFACT_DIR"/* target/docker/ 2>/dev/null || {
                echo "⚠️ 直接复制失败，创建模拟构建"
              }

              # 检查是否成功复制了 tinyMediaManager 目录
              if [ -d "target/docker/tinyMediaManager" ]; then
                echo "✅ 成功复制构建产物目录"
                HAS_ARTIFACTS=true
              fi
            fi
          fi
        else
          echo "⚠️ 未找到构建产物目录，检查 artifacts 目录..."

          # 检查传统的 artifacts 目录
          if [ -d "artifacts" ]; then
            echo "📦 处理 artifacts 目录..."
            cd artifacts

            # 查找所有可能的构建产物
            echo "🔍 查找AMD64构建产物..."
            BUILD_FILES=$(find . -type f \( \
              -name "*linux*amd64*" -o \
              -name "*amd64*linux*" -o \
              -name "*tinyMediaManager*amd64*" -o \
              -name "*tinyMediaManager*linux*amd64*" \
            \) | head -10)

            if [ -n "$BUILD_FILES" ]; then
              echo "✅ 找到构建产物:"
              echo "$BUILD_FILES"

              # 处理第一个找到的文件
              BUILD_FILE=$(echo "$BUILD_FILES" | head -1)
              echo "📦 处理文件: $BUILD_FILE"

              # 创建目标目录
              mkdir -p ../target/docker/tinyMediaManager

              # 根据文件类型处理
              case "$BUILD_FILE" in
                *.tar.*)
                  echo "📦 解压 tar 文件: $BUILD_FILE"
                  tar xf "$BUILD_FILE" -C ../target/docker/ || {
                    echo "❌ tar 解压失败"
                    exit 1
                  }
                  ;;
                *.zip)
                  echo "📦 解压 zip 文件: $BUILD_FILE"
                  unzip -q "$BUILD_FILE" -d ../target/docker/ || {
                    echo "❌ zip 解压失败"
                    exit 1
                  }
                  ;;
                *)
                  echo "📋 直接复制文件: $BUILD_FILE"
                  cp "$BUILD_FILE" ../target/docker/tinyMediaManager/
                  ;;
              esac

              cd ..
              HAS_ARTIFACTS=true
            else
              echo "⚠️ artifacts 目录中未找到上游构建产物"
              cd ..
            fi
          else
            echo "⚠️ 无上游构建产物，使用本地构建"
          fi
        fi

        # 如果没有构建产物，则创建模拟构建
        if [ "$HAS_ARTIFACTS" = "false" ]; then
          echo "⚠️ 创建模拟构建环境"
          mkdir -p target/docker/tinyMediaManager

          # 创建模拟tinyMediaManager二进制文件
          echo '#!/bin/bash' > target/docker/tinyMediaManager/tinyMediaManager
          echo 'echo "tinyMediaManager v5.2.0 - Docker Build"' >> target/docker/tinyMediaManager/tinyMediaManager
          echo 'echo "This is a placeholder binary for testing"' >> target/docker/tinyMediaManager/tinyMediaManager
          chmod +x target/docker/tinyMediaManager/tinyMediaManager
        fi

        # 复制Dockerfile
        if [ -f "src/docker/Dockerfile.amd64" ]; then
          cp src/docker/Dockerfile.amd64 target/docker/Dockerfile
          echo "✅ 成功复制Dockerfile.amd64"
        else
          echo "❌ 无法找到src/docker/Dockerfile.amd64"
          echo "📁 src/docker目录内容:"
          ls -la src/docker/
          exit 1
        fi

        echo "✅ Docker上下文准备完成"
        echo "📁 最终目录结构:"
        find target/docker -type f

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push AMD64 image
      uses: docker/build-push-action@v5
      with:
        context: target/docker
        file: target/docker/Dockerfile
        platforms: linux/amd64
        push: true
        tags: |
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-amd64
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.TMM_VERSION }}-amd64

  # 构建 ARM64 Docker 镜像
  docker-build-arm64:
    name: Build Docker Image (ARM64)
    runs-on: ubuntu-latest
    needs: check-environment
    # 只在明确需要构建时执行（防止重复触发）
    if: needs.check-environment.outputs.should-build == 'true'
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: devel

    - name: Download Linux ARM64 artifact (if available)
      if: needs.check-environment.outputs.has-upstream-artifacts == 'true' && github.event.workflow_run.conclusion == 'success'
      uses: actions/download-artifact@v4
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        run-id: ${{ github.event.workflow_run.id }}
        pattern: "tinyMediaManager-linux-arm64*"
        path: artifacts

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Prepare Docker context
      run: |
        echo "🏗️ Preparing Docker context for ARM64..."
        mkdir -p target/docker

        # 调试信息
        echo "📁 当前工作目录: $(pwd)"
        echo "📁 src/docker目录内容:"
        ls -la src/docker/
        echo "📁 当前目录所有文件:"
        find . -name "*Dockerfile*" -type f

        # 显示下载的构建产物
        echo "📁 下载的构建产物:"
        ls -la . | grep -E "(tinyMediaManager|tinymediamanager)" || echo "未找到构建产物目录"

        # 检查是否有构建产物
        HAS_ARTIFACTS=false

        # 首先检查是否有下载的构建产物目录（GitHub Actions download-artifact 会创建目录）
        ARTIFACT_DIRS=$(find . -maxdepth 1 -type d -name "*linux*arm64*" -o -name "*tinyMediaManager*arm64*" | head -5)

        if [ -n "$ARTIFACT_DIRS" ]; then
          echo "📦 找到构建产物目录:"
          echo "$ARTIFACT_DIRS"

          # 处理第一个找到的目录
          ARTIFACT_DIR=$(echo "$ARTIFACT_DIRS" | head -1)
          echo "📂 处理目录: $ARTIFACT_DIR"

          # 显示目录内容
          echo "📁 目录内容:"
          ls -la "$ARTIFACT_DIR"

          # 查找目录中的构建产物文件
          BUILD_FILES=$(find "$ARTIFACT_DIR" -type f \( \
            -name "*.tar.xz" -o \
            -name "*.tar.gz" -o \
            -name "*.zip" -o \
            -name "*tinyMediaManager*" \
          \) | head -10)

          if [ -n "$BUILD_FILES" ]; then
            echo "✅ 找到构建产物文件:"
            echo "$BUILD_FILES"

            # 处理第一个找到的文件
            BUILD_FILE=$(echo "$BUILD_FILES" | head -1)
            echo "📦 处理文件: $BUILD_FILE"

            # 创建目标目录
            mkdir -p target/docker

            # 根据文件类型处理
            case "$BUILD_FILE" in
              *.tar.xz)
                echo "📦 解压 tar.xz 文件: $BUILD_FILE"
                tar xJf "$BUILD_FILE" -C target/docker/ || {
                  echo "❌ tar.xz 解压失败"
                  exit 1
                }
                ;;
              *.tar.gz)
                echo "📦 解压 tar.gz 文件: $BUILD_FILE"
                tar xzf "$BUILD_FILE" -C target/docker/ || {
                  echo "❌ tar.gz 解压失败"
                  exit 1
                }
                ;;
              *.zip)
                echo "📦 解压 zip 文件: $BUILD_FILE"
                unzip -q "$BUILD_FILE" -d target/docker/ || {
                  echo "❌ zip 解压失败"
                  exit 1
                }
                ;;
              *)
                echo "📋 直接复制文件: $BUILD_FILE"
                mkdir -p target/docker/tinyMediaManager
                cp "$BUILD_FILE" target/docker/tinyMediaManager/
                ;;
            esac

            HAS_ARTIFACTS=true
          else
            echo "⚠️ 目录中未找到可识别的构建产物文件"
            echo "📁 尝试直接复制整个目录内容..."

            # 如果没有找到特定文件，尝试复制整个目录
            if [ -d "$ARTIFACT_DIR" ]; then
              mkdir -p target/docker
              cp -r "$ARTIFACT_DIR"/* target/docker/ 2>/dev/null || {
                echo "⚠️ 直接复制失败，创建模拟构建"
              }

              # 检查是否成功复制了 tinyMediaManager 目录
              if [ -d "target/docker/tinyMediaManager" ]; then
                echo "✅ 成功复制构建产物目录"
                HAS_ARTIFACTS=true
              fi
            fi
          fi
        else
          echo "⚠️ 未找到构建产物目录，检查 artifacts 目录..."

          # 检查传统的 artifacts 目录
          if [ -d "artifacts" ]; then
            echo "📦 处理 artifacts 目录..."
            cd artifacts

            # 查找所有可能的构建产物
            echo "🔍 查找ARM64构建产物..."
            BUILD_FILES=$(find . -type f \( \
              -name "*linux*arm64*" -o \
              -name "*arm64*linux*" -o \
              -name "*tinyMediaManager*arm64*" -o \
              -name "*tinyMediaManager*linux*arm64*" \
            \) | head -10)

            if [ -n "$BUILD_FILES" ]; then
              echo "✅ 找到构建产物:"
              echo "$BUILD_FILES"

              # 处理第一个找到的文件
              BUILD_FILE=$(echo "$BUILD_FILES" | head -1)
              echo "📦 处理文件: $BUILD_FILE"

              # 创建目标目录
              mkdir -p ../target/docker/tinyMediaManager

              # 根据文件类型处理
              case "$BUILD_FILE" in
                *.tar.*)
                  echo "📦 解压 tar 文件: $BUILD_FILE"
                  tar xf "$BUILD_FILE" -C ../target/docker/ || {
                    echo "❌ tar 解压失败"
                    exit 1
                  }
                  ;;
                *.zip)
                  echo "📦 解压 zip 文件: $BUILD_FILE"
                  unzip -q "$BUILD_FILE" -d ../target/docker/ || {
                    echo "❌ zip 解压失败"
                    exit 1
                  }
                  ;;
                *)
                  echo "📋 直接复制文件: $BUILD_FILE"
                  cp "$BUILD_FILE" ../target/docker/tinyMediaManager/
                  ;;
              esac

              cd ..
              HAS_ARTIFACTS=true
            else
              echo "⚠️ artifacts 目录中未找到上游构建产物"
              cd ..
            fi
          else
            echo "⚠️ 无上游构建产物，使用本地构建"
          fi
        fi

        # 如果没有构建产物，则创建模拟构建
        if [ "$HAS_ARTIFACTS" = "false" ]; then
          echo "⚠️ 创建模拟构建环境"
          mkdir -p target/docker/tinyMediaManager

          # 创建模拟tinyMediaManager二进制文件
          echo '#!/bin/bash' > target/docker/tinyMediaManager/tinyMediaManager
          echo 'echo "tinyMediaManager v5.2.0 - Docker Build"' >> target/docker/tinyMediaManager/tinyMediaManager
          echo 'echo "This is a placeholder binary for testing"' >> target/docker/tinyMediaManager/tinyMediaManager
          chmod +x target/docker/tinyMediaManager/tinyMediaManager
        fi

        # 复制Dockerfile
        if [ -f "src/docker/Dockerfile.arm64" ]; then
          cp src/docker/Dockerfile.arm64 target/docker/Dockerfile
          echo "✅ 成功复制Dockerfile.arm64"
        else
          echo "❌ 无法找到src/docker/Dockerfile.arm64"
          echo "📁 src/docker目录内容:"
          ls -la src/docker/
          exit 1
        fi

        echo "✅ Docker上下文准备完成"
        echo "📁 最终目录结构:"
        find target/docker -type f

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push ARM64 image
      uses: docker/build-push-action@v5
      with:
        context: target/docker
        file: target/docker/Dockerfile
        platforms: linux/arm64
        push: true
        tags: |
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-arm64
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.TMM_VERSION }}-arm64

  # 创建多架构manifest
  create-manifest:
    name: Create Multi-Architecture Manifest
    runs-on: ubuntu-latest
    needs: [docker-build-amd64, docker-build-arm64]
    if: always()
    permissions:
      contents: read
      packages: write

    steps:
    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Create and push manifest
      run: |
        echo "🐳 创建多架构manifest..."
        
        # 拉取各个架构的镜像
        docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-amd64 || true
        docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-arm64 || true
        
        # 创建manifest
        docker manifest create ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-amd64 \
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-arm64 || true
        
        # 推送manifest
        docker manifest push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest || true
        
        echo "✅ 多架构manifest创建完成"
